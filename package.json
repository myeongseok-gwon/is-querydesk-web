{
  "name": "is-querydesk-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Public, in-browser semantic search over the IS corpus (journals / conferences / preprints).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@clerk/clerk-js": "^6.14.0",
    "@huggingface/transformers": "^3.3.0",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
